# jenkins.yaml.tpl 구조 예시 (민감값은 terraform.tfvars / CI 시크릿으로 주입)
#
# jenkins:
#   securityRealm:
#     local:
#       allowsSignup: false
#       users:
#         - id: "admin"
#           password: "<jenkins_casc_password>"
# credentials:
#   system:
#     domainCredentials:
#       - credentials:
#           - string:
#               id: "github-token"
#               secret: "<jenkins_github_token>"
# aws-credentials: Jenkins UI에서 추가하거나 Jenkins EC2 IAM Role 사용 (CasC 미포함)
